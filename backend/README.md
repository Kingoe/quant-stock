# 后端

本目录用于存放 Quant Stock 的 Python 后端。

## 当前范围

当前仅建立 Python 项目骨架：

- `app/`：后端应用代码目录。
- `tests/`：后端测试目录。

后续任务会按 `docs/TASK_PLAN.md` 的顺序继续添加依赖配置、配置读取、SQLite 连接层和健康检查接口。

## 约束

- 不在本目录直接提交虚拟环境。
- 不提交本地数据库、缓存和日志。
- 业务代码必须配套测试或明确验证方式。
- 每轮开发必须同步更新任务规划和更新日志。
