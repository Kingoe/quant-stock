# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：因子计算
- 当前任务：M4-3 实现质量因子
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 4 的第三个任务

## 本轮验收标准

本轮完成必须满足：

- 实现质量因子，基于 ROE、毛利率、现金流质量计算。
- 使用对齐后的财务数据。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M4-3 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮变更。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮实现 M4-4 成长因子。

开始前应先更新本文档：

- 当前任务改为：M4-4 实现成长因子
- 明确 M4-4 验收标准和验证命令