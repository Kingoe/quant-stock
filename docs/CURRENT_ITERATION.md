# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：组合构建
- 当前任务：M5-3 应用行业仓位上限
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 5 的第三个任务
- 当前结果：已完成，下一轮进入 M5-4 生成买入、卖出、持有、观察列表

## 本轮验收标准

本轮完成必须满足：

- 实现行业仓位上限工具。
- 同一行业累计目标仓位不得超过行业上限。
- 同行业内排名靠前的股票优先保留目标仓位。
- 保留候选股排名、总分和目标仓位，便于后续生成调仓建议。
- 只处理行业约束，不生成买卖动作。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M5-3 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（已完成）
- Git 工作区提交后保持干净。（提交后检查）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_factor_calculators.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮实现 M5-4：生成买入、卖出、持有、观察列表。

开始前应先更新本文档：

- 当前任务改为：M5-4 生成买入、卖出、持有、观察列表
- 明确 M5-4 验收标准和验证命令
