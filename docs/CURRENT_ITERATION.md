# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：数据底座
- 当前任务：M2-2 实现交易日历加载
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 2 的第二个任务

## 本轮验收标准

本轮完成必须满足：

- 创建 `backend/app/data/` 数据加载模块。
- 定义交易日历记录结构。
- 支持把交易日历记录写入 `trading_calendar` 表。
- 支持重复加载同一日期时更新记录。
- 自动维护上一交易日和下一交易日字段。
- 支持按日期区间查询开市交易日。
- 支持查询给定日期之后的下一个开市交易日。
- 先写失败测试，再实现代码。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M2-2 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮变更。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_trading_calendar_loader.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮开始 M2-3：实现股票基础信息加载。

开始前应先更新本文档：

- 当前阶段改为：数据底座
- 当前任务改为：M2-3 实现股票基础信息加载
- 明确 M2-3 验收标准和验证命令
