# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：前端运营化页面
- 当前任务：M11-5 添加券商准入评估 API
- 任务来源：按 `docs/TASK_PLAN.md` 的最早未完成任务顺序推进
- 对任务顺序的影响：M11-5 已补齐，下一轮进入 M11-6
- 当前结果：已完成，新增券商准入评估 API

## 本轮验收标准

本轮完成必须满足：

- 新增 `GET /api/broker/readiness`。
- 返回准入状态、失败原因、允许动作和是否允许进入人工小资金试点评审。
- 默认状态必须阻止进入试点，不开放自动下单能力。
- 满足准入条件时也只能进入人工试点评审，不能返回真实下单动作。
- API 文案明确“不是实盘交易入口”。
- 新增后端 API 测试覆盖默认阻止、准入通过和参数校验。
- `docs/TASK_PLAN.md` 将 M11-5 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮核验和修复结果。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 运行 `uv run ruff format --check app tests`。
- 运行 `npm test`。
- 运行 `npm run lint`。
- 使用 Node.js 20.19 或更高版本运行 `npm run build`。
- 检查任务规划、当前迭代和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮按最早未完成任务继续：

- M11-6 添加券商准入评估页面。
