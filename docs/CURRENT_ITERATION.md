# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：数据底座
- 当前任务：M2-4 实现中证 800 成分股加载
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 2 的第四个任务

## 本轮验收标准

本轮完成必须满足：

- 定义指数成分记录结构。
- 支持把中证 800 成分股写入 `index_constituents` 表。
- 支持重复加载同一指数、股票、日期时更新权重。
- 支持按指数代码和日期查询成分股代码。
- 支持查询给定日期可用的最新中证 800 成分股。
- 正确保存指数代码、股票代码、交易日期和权重字段。
- 先写失败测试，再实现代码。
- 不接入外部数据源。
- 不请求实时网络数据。
- 不实现股票池过滤规则。
- `docs/TASK_PLAN.md` 将 M2-4 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮变更。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_index_constituents_loader.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮开始 M2-5：实现日行情加载。

开始前应先更新本文档：

- 当前阶段改为：数据底座
- 当前任务改为：M2-5 实现日行情加载
- 明确 M2-5 验收标准和验证命令
