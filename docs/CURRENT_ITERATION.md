# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：组合构建
- 当前任务：M5-4 生成买入、卖出、持有、观察列表
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 5 的第四个任务
- 当前结果：已完成，下一轮进入 M5-5 添加交易可用性提示

## 本轮验收标准

本轮完成必须满足：

- 实现调仓建议数据模型。
- 新进入目标组合的股票进入买入列表。
- 当前持仓但不在目标组合的股票进入卖出列表。
- 当前持仓且仍在目标组合的股票进入持有列表。
- 高分但未入选最终组合的股票进入观察列表。
- 支持按 action 排序输出：buy、hold、sell、watch。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M5-4 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（待更新）
- Git 工作区提交后保持干净。（待提交）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_portfolio_candidates.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮实现 M5-5：添加交易可用性提示。

开始前应先更新本文档：

- 当前任务改为：M5-5 添加交易可用性提示
- 明确 M5-5 验收标准和验证命令