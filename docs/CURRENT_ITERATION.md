# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：后续增强
- 当前任务：M10-4 添加参数实验跟踪
- 任务来源：按 `docs/TASK_PLAN.md` 的最早未完成任务顺序推进
- 对任务顺序的影响：M10-4 已补齐，下一轮进入 M10-5
- 当前结果：已完成，新增参数实验记录表和查询工具

## 本轮验收标准

本轮完成必须满足：

- 新增参数实验记录表，保存实验名称、说明、参数、指标、备注和创建时间。
- 参数和指标使用 JSON 保存，方便保留完整调参上下文。
- 能按实验 ID 查询完整实验记录。
- 能按创建时间倒序列出最近实验，并支持数量限制。
- 空实验名称必须显式报错。
- 参数或指标不可 JSON 序列化时必须显式报错。
- `docs/TASK_PLAN.md` 将 M10-4 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮核验和修复结果。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 运行 `uv run ruff format --check app tests`。
- 运行 `npm test`。
- 运行 `npm run lint`。
- 使用 Node.js 20.19 或更高版本运行 `npm run build`。
- 检查任务规划、当前迭代和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮按最早未完成任务继续：

- M10-5 添加通知渠道。
