# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：前端运营化页面
- 当前任务：M11-2 添加实验记录页面
- 任务来源：按 `docs/TASK_PLAN.md` 的最早未完成任务顺序推进
- 对任务顺序的影响：M11-2 已补齐，下一轮进入 M11-3
- 当前结果：已完成，新增实验记录导航和前端页面

## 本轮验收标准

本轮完成必须满足：

- 前端新增“实验记录”导航入口。
- 实验记录页展示实验名称、说明、创建时间、核心指标、备注和参数详情。
- 空数据时展示清晰空状态。
- 接口失败时使用稳定兜底空状态，不阻塞页面。
- 新增前端测试覆盖导航、实验展示、空状态和失败兜底。
- `docs/TASK_PLAN.md` 将 M11-2 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮核验和修复结果。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 运行 `uv run ruff format --check app tests`。
- 运行 `npm test`。
- 运行 `npm run lint`。
- 使用 Node.js 20.19 或更高版本运行 `npm run build`。
- 检查任务规划、当前迭代和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮按最早未完成任务继续：

- M11-3 添加通知历史 API。
