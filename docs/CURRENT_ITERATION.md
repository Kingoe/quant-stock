# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：组合构建
- 当前任务：M5-6 添加调仓接口和测试
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 5 的第六个任务
- 当前结果：已完成，里程碑 5 组合构建全部完成，下一轮进入里程碑 6 回测引擎

## 本轮验收标准

本轮完成必须满足：

- 添加 `GET /api/rebalance/latest` 接口。
- 接口返回买入、卖出、持有、观察列表。
- 遵守 API 约定格式（data + meta）。
- 接口参数支持：指数代码、评分日期、数据库 URL、候选数量、仓位限制。
- 接口参数验证：limit 范围 1-50、权重 0-1。
- 支持空数据返回（缺少指数成分）。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M5-6 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（待更新）
- Git 工作区提交后保持干净。（待提交）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_rebalance_api.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

里程碑 5（组合构建）已全部完成。

下一轮进入里程碑 6：回测引擎。

开始下一轮前应先更新本文档：

- 当前任务改为：M6-1 实现周频调仓调度
- 明确 M6-1 验收标准和验证命令
