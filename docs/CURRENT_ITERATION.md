# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：因子计算
- 当前任务：M4-7 实现流动性因子
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 4 的第七个任务
- 当前结果：已完成，下一轮进入 M4-8 实现去极值和排名

## 本轮验收标准

本轮完成必须满足：

- 实现流动性因子，基于近 20 日平均成交额计算。
- 使用对齐后的日行情数据。
- 成交额越高，流动性得分越高。
- 支持平均成交额权重配置。
- 对缺失或不足 20 日成交额历史保持可解释的默认处理。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M4-7 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（已完成）
- Git 工作区提交后保持干净。（提交后检查）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_factor_calculators.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮实现 M4-8：实现去极值和排名。

开始前应先更新本文档：

- 当前任务改为：M4-8 实现去极值和排名
- 明确 M4-8 验收标准和验证命令
