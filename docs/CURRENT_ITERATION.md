# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：后续增强
- 当前任务：M10-3 添加行业中性化
- 任务来源：按 `docs/TASK_PLAN.md` 的最早未完成任务顺序推进
- 对任务顺序的影响：M10-3 已补齐，下一轮进入 M10-4
- 当前结果：已完成，新增行业内排名中性化工具

## 本轮验收标准

本轮完成必须满足：

- 支持按股票行业分组计算行业内相对分数。
- 同一行业内高分股票中性化后仍高于低分股票。
- 并列分数使用平均排名，避免股票代码顺序造成偏差。
- 行业样本过小时返回中性分，避免单只股票行业被错误放大。
- 缺失行业统一归入“未知”行业。
- 非法最小行业样本数必须显式报错。
- `docs/TASK_PLAN.md` 将 M10-3 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮核验和修复结果。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 运行 `uv run ruff format --check app tests`。
- 运行 `npm test`。
- 运行 `npm run lint`。
- 使用 Node.js 20.19 或更高版本运行 `npm run build`。
- 检查任务规划、当前迭代和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮按最早未完成任务继续：

- M10-4 添加参数实验跟踪。
