# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：数据底座
- 当前任务：M2-8 实现数据状态检查
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 2 的第八个任务

## 本轮验收标准

本轮完成必须满足：

- 实现数据状态检查接口，支持查询最新行情日期、最新财务数据日期。
- 实现数据完整性检查，支持检查是否有缺失数据。
- 支持按数据类型查询最新可用日期（日行情、估值、财务等）。
- 支持返回数据状态汇总信息。
- 先写失败测试，再实现代码。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M2-8 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮变更。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_data_status.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮开始 M3-1：实现中证 800 基础股票池查询。

开始前应先更新本文档：

- 当前阶段改为：股票池过滤
- 当前任务改为：M3-1 实现中证 800 基础股票池查询
- 明确 M3-1 验收标准和验证命令