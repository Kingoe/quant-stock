# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：因子计算
- 当前任务：M4-9 实现加权总分
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 4 的第九个任务
- 当前结果：已完成，下一轮进入 M5-1 选择排名靠前候选股

## 本轮验收标准

本轮完成必须满足：

- 实现可复用的加权总分计算工具。
- 使用第一版初始均衡权重：估值 25%、质量 25%、成长 20%、动量 20%、风险 5%、流动性 5%。
- 支持自定义权重，并校验权重非负且总和为 1。
- 对缺失分组因子得分使用中性分 0.5。
- 提供调用现有因子计算器的总分入口。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M4-9 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（已完成）
- Git 工作区提交后保持干净。（提交后检查）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_factor_calculators.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮进入里程碑 5：组合构建。

开始前应先更新本文档：

- 当前任务改为：M5-1 选择排名靠前候选股
- 明确 M5-1 验收标准和验证命令
