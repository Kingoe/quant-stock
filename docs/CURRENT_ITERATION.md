# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：组合构建
- 当前任务：M5-1 选择排名靠前候选股
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 5 的第一个任务
- 当前结果：已完成，下一轮进入 M5-2 应用单票仓位上限

## 本轮验收标准

本轮完成必须满足：

- 实现候选股选择工具，按总分从高到低排序。
- 默认选择排名靠前的股票，数量由 `limit` 参数控制。
- 相同总分时按股票代码稳定排序，保证结果可复现。
- 忽略没有总分的股票。
- 只选择候选股，不处理仓位、行业约束和调仓动作。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M5-1 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（已完成）
- Git 工作区提交后保持干净。（提交后检查）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_factor_calculators.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮实现 M5-2：应用单票仓位上限。

开始前应先更新本文档：

- 当前任务改为：M5-2 应用单票仓位上限
- 明确 M5-2 验收标准和验证命令
