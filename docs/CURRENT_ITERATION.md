# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：股票池过滤
- 当前任务：M3-1 实现中证 800 基础股票池查询
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 3 的第一个任务

## 本轮验收标准

本轮完成必须满足：

- 实现按调仓日期查询中证 800 基础股票池。
- 支持使用该日期可用的最新指数成分。
- 返回股票代码列表，确保是活跃股票。
- 先写失败测试，再实现代码。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M3-1 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮变更。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_universe.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮开始 M3-2：实现 ST 过滤。

开始前应先更新本文档：

- 当前阶段改为：股票池过滤
- 当前任务改为：M3-2 实现 ST 过滤
- 明确 M3-2 验收标准和验证命令