# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：组合构建
- 当前任务：M5-5 添加交易可用性提示
- 任务来源：按 `docs/TASK_PLAN.md` 的里程碑和任务 ID 顺序推进
- 对任务顺序的影响：正常进入里程碑 5 的第五个任务
- 当前结果：已完成，下一轮进入 M5-6 添加调仓接口和测试

## 本轮验收标准

本轮完成必须满足：

- 实现 TradingStatus 数据结构，包含涨停、跌停、停牌状态。
- 实现 add_trading_availability_notes 函数，为调仓建议添加风险提示。
- 涨停股票的买入建议应标记风险（涨停无法买入）。
- 跌停股票的卖出建议应标记风险（跌停无法卖出）。
- 停牌股票的买入/卖出建议应标记风险（停牌无法交易）。
- 跌停不阻止买入，涨停不阻止卖出。
- 不破坏现有调仓建议生成逻辑。
- 不接入外部数据源。
- 不请求实时网络数据。
- `docs/TASK_PLAN.md` 将 M5-5 标记为 `Done`。（已完成）
- `docs/CHANGELOG.md` 记录本轮变更。（待更新）
- Git 工作区提交后保持干净。（待提交）

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest tests/test_portfolio_candidates.py`。
- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 检查任务规划和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮实现 M5-6：添加调仓接口和测试。

开始前应先更新本文档：

- 当前任务改为：M5-6 添加调仓接口和测试
- 明确 M5-6 验收标准和验证命令