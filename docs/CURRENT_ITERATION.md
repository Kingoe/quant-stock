# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：后续增强
- 当前任务：M10-2 添加因子有效性分析
- 任务来源：按 `docs/TASK_PLAN.md` 的最早未完成任务顺序推进
- 对任务顺序的影响：M10-2 已补齐，下一轮进入 M10-3
- 当前结果：已完成，新增未来收益、IC、Rank IC 和分层收益分析工具

## 本轮验收标准

本轮完成必须满足：

- 未来收益计算优先使用复权收盘价，缺失时回退普通收盘价。
- 未来收益必须使用评分日之后的行情，不能把评分日价格当成未来价格。
- IC 使用因子值与未来收益的 Pearson 相关系数。
- Rank IC 使用因子值排名与未来收益排名的相关系数。
- 分层收益按因子分数从高到低稳定分组。
- 样本不足或常数序列时相关系数返回空值，不伪造结果。
- `docs/TASK_PLAN.md` 将 M10-2 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮核验和修复结果。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 运行 `uv run ruff format --check app tests`。
- 运行 `npm test`。
- 运行 `npm run lint`。
- 使用 Node.js 20.19 或更高版本运行 `npm run build`。
- 检查任务规划、当前迭代和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮按最早未完成任务继续：

- M10-3 添加行业中性化。
