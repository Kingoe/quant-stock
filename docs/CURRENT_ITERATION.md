# 当前迭代

本文档记录当前正在执行或即将执行的一轮任务。每轮迭代开始时更新，完成后保留结果或清空为下一轮准备。

## 当前状态

- 当前阶段：前端运营化页面
- 当前任务：M11-6 添加券商准入评估页面
- 任务来源：按 `docs/TASK_PLAN.md` 的最早未完成任务顺序推进
- 对任务顺序的影响：M11-6 已补齐，M11 前端运营化页面全部完成
- 当前结果：已完成，新增券商准入评估页面

## 本轮验收标准

本轮完成必须满足：

- 新增前端“券商准入”导航入口。
- 新增券商准入评估页面，展示当前准入状态。
- 明确展示“不自动下单”和“不是实盘交易入口”。
- 展示准入条件、失败原因和只读允许动作。
- 接口失败时展示 blocked 兜底状态。
- 新增前端测试覆盖阻止状态、准入通过状态、接口失败兜底和导航切换。
- `docs/TASK_PLAN.md` 将 M11-6 标记为 `Done`。
- `docs/CHANGELOG.md` 记录本轮核验和修复结果。
- Git 工作区提交后保持干净。

## 本轮验证方式

本轮验证方式：

- 运行 `uv run pytest`。
- 运行 `uv run ruff check app tests`。
- 运行 `uv run ruff format --check app tests`。
- 运行 `npm test`。
- 运行 `npm run lint`。
- 使用 Node.js 20.19 或更高版本运行 `npm run build`。
- 检查任务规划、当前迭代和更新日志是否同步。
- 检查 Git 状态。

## 下一轮建议

下一轮建议：

- M11 已全部完成。下一轮先整理和确认新的里程碑范围，再继续按任务顺序推进。
